# Xdfile Manager TODO - PR 级可执行路线

> 事实基线更新至 2026-06-08，以当前工作区可验证状态为准。
> 本文档是后续开发者或 agent 可直接执行的工程路线，不是功能完成状态清单。
>
> 当前工作区可能存在未提交或未跟踪改动；任何任务是否完成，以对应代码、测试、文档和验收命令全部满足为准。

## 1. Summary

Xdfile Manager 的短中期改造应先稳住构建、测试、配置边界和文档事实，再逐步补齐差异化文件管理能力。目标读者是后续开发者或 agent：打开任一任务即可知道为什么做、改哪里、怎么测、什么算完成、什么明确不做。

## 2. 产品定位

- **TUI-first**：现代体验优先在终端内完成，不把 GUI 化作为产品方向。
- **双栏文件工作台**：左右面板是复制、移动、对照、远程传输的主工作流。
- **底部终端**：托管 PTY/命令输入与文件面板共享上下文，减少 shell 与文件管理器之间的切换成本。
- **远程 NetBox**：保留 SSH/NetBox 的远程目录浏览、远程命令和 tar 流式传输方向。
- **F2 宏菜单**：保留类 Far Manager 的 metasymbol 菜单能力。
- **强键鼠工作流**：键盘优先，同时保持鼠标点击、双击、右键、滚轮等操作。

### 非目标

- 不改造成 GUI 文件管理器。
- 不用 superfile 兼容性覆盖 Xdfile 的 PTY、远程、F2 宏菜单等差异化能力。
- 不在破坏性文件操作缺少测试保护时做大重构。
- 不把 AI Agent、动态插件 / 不受限插件列为短期承诺；AI 命令草稿和外部命令插件只按 v1 安全边界推进。
- 不承诺旧 superfile 配置字段全部有效。

## 3. 事实基线

| 领域 | 2026-06-08 基线事实 |
|---|---|
| 技术栈 | Go；Bubble Tea fork、Lip Gloss、Bubbles；入口为 `main.go` 和 `src/cmd/main.go`。 |
| 主工作流 | 双栏文件面板、底部终端、F2 User Menu、NetBox 远程目录、文件预览和主题菜单已形成核心体验。 |
| 测试现状 | 已有 shell、文件操作、剪贴板、NetBox tar 安全、UI 状态机、AI、插件、workspace tab、主题、归档、Pin、keymap、过滤 / 搜索、批量重命名、Zoxide、MD5 等测试；不能再使用测试空白这类过期说法。 |
| Git 卫生 | 工作区已新增 `.gitignore`，并从工作树移除根目录 `xdfile-manager` 二进制；构建产物位于已忽略的 `dist/`。 |
| Release | release workflow 在 `v*` tag push 或手动 `tag_name` 发布前先跑 `go test ./...` 和 `go vet ./...`，再构建 release 矩阵产物。 |
| 构建脚本 | `scripts/build.sh` 和 `scripts/build.ps1` 会先跑 test/vet，再构建 Linux amd64、Windows amd64、Darwin amd64/arm64。 |
| 配置 | TOML 配置资产存在；运行时只接通稳定白名单字段，其他旧 superfile 字段保留为 legacy/reserved；布局、User Menu、NetBox、history、Pin、workspace tabs 等状态继续主要保存为 JSON。 |
| 主题 | Persona 主题和 TOML 主题资产都可进入 Theme 菜单；TOML 主题会映射到当前 TUI token，并对缺失、非法和低对比值 fallback。 |
| 远程限制 | 本地/远程面板间 `F5` 复制已接通；跨端 `F6` 移动、remote-to-remote 面板复制、远程删除撤销仍明确不做。 |

## 4. 执行原则

- 一个任务优先对应一个 PR；确需拆分时，按本文档中的子任务 ID 分开提交。
- 实施任务时先确认当前工作区状态，不能覆盖用户或其他 agent 已有改动。
- P0 先修构建、测试和事实边界，不做大规模 UI 或目录重构。
- 涉及删除、移动、覆盖、远程写入的任务必须先补测试护栏。
- 文档中的新能力必须能在源码或测试中找到证据。
- 旧 superfile 字段默认视为 `legacy/reserved`，除非本路线明确把它接入运行时。
- 批量重命名、过滤、模糊搜索、Zoxide、MD5 分别按独立任务推进，不混成一个 PR。

## 5. P0 - 地基与正确性

### P0-1 Git 与构建卫生

| 字段 | 内容 |
|---|---|
| ID | `P0-1` |
| 目标 | 让构建产物不污染工作区，并让发布流程在构建前先验证质量。 |
| 范围 | 只处理 git 忽略规则、已跟踪二进制、CI release workflow、本地 build 脚本。 |
| 关键改动 | 新增 `.gitignore`；从 git 索引移除根目录 `xdfile-manager` 二进制；忽略 `/dist/`、本地二进制、`*.exe`、`xdfile-data/`、临时预览和缓存产物；release build 前加入 `go test ./...` 和 `go vet ./...`；修正 release tag 策略为 `v*` tag push 或手动显式版本；让 `scripts/build.sh` 和 `scripts/build.ps1` 至少覆盖 Linux amd64、Windows amd64、Darwin amd64/arm64。 |
| 测试 | `go test ./...`；`go vet ./...`；运行本地 build 脚本或等价 dry run；重新构建后检查 `git status --short`。 |
| 验收 | 构建后工作区不出现未忽略的二进制产物；CI 日志包含 test/vet；release tag 来源清晰；README、脚本、CI 的构建目标一致。 |
| 不做 | 不改业务代码；不重写发布系统；不引入 goreleaser 等新发布工具。 |

### P0-2 破坏性文件操作测试

| 字段 | 内容 |
|---|---|
| ID | `P0-2` |
| 目标 | 在删除、撤销、复制、移动、粘贴、远程 tar 这类高风险路径上建立测试护栏。 |
| 范围 | 只补单元测试和必要的测试辅助注入点；不改变用户可见行为。 |
| 关键改动 | 覆盖删除 staging、确认、取消、undo 恢复；覆盖复制/移动防御，包括复制目录到自身、目标已存在、跨盘 move fallback；覆盖粘贴冲突 `Replace`、`Skip`、`Keep both`、`Apply all`；覆盖剪切粘贴完成后的剪贴板状态；覆盖 NetBox tar entry 防路径穿越、URL encode/decode；必要时增加最小可替换函数变量以便 fake 文件系统、NetBox 或平台调用。 |
| 测试 | 新增 `src/cmd/*_test.go`；使用 `t.TempDir()` 构造本地文件树；NetBox 和平台调用使用 fake；运行 `go test ./...`。 |
| 验收 | 新测试不依赖真实 SSH、真实系统剪贴板或用户目录；破坏性行为回归会导致测试失败；`go test ./...` 通过。 |
| 不做 | 不改文件操作语义；不做端到端 TUI 自动化；不依赖真实远程主机。 |

### P0-3 配置边界收敛

| 字段 | 内容 |
|---|---|
| ID | `P0-3` |
| 目标 | 让用户和开发者能区分当前有效配置与旧 superfile 遗留字段，避免误以为所有 TOML 字段都已生效。 |
| 范围 | 只接通少量稳定字段；运行时偏好继续主要保存在 JSON layout 和现有状态文件。 |
| 关键改动 | 启动流程安全加载 `xdfile-config.toml` 的白名单字段；初始白名单只包含可验证字段：`theme`、`default_directory`、`default_open_file_preview`、`show_image_preview`、`enable_file_preview_border`、`nerdfont`；明确优先级：CLI 路径参数 > layout startup path > config default directory；默认配置中把未接通字段标为 `legacy/reserved`；README 不把保留字段列为可用功能。 |
| 测试 | 配置加载、缺字段兼容、非法字段值、路径优先级、layout/theme 优先级测试；运行 `go test ./...` 和 `go vet ./...`。 |
| 验收 | 修改白名单字段后重启可观察到对应变化；旧配置文件不会因缺失新字段破坏启动；未接通字段不会出现在可用功能承诺中。 |
| 不做 | 不承诺完整兼容 superfile；不一次性接通 sidebar、zoxide、MD5、metadata、theme 全字段。 |

### P0-4 文档事实清理

| 字段 | 内容 |
|---|---|
| ID | `P0-4` |
| 目标 | 让 README、todo 和源码注释描述当前事实，不继续传播过期说法。 |
| 范围 | README、todo、失效注释、构建说明；只改文档或注释。 |
| 关键改动 | 修正测试现状；修正 CI/release 现状；修正配置/主题接通状态；清理误导性的 trash 来源注释；删除旧测试数量、旧远程能力、旧构建目标等过期说法。 |
| 测试 | `go test ./...`；`go vet ./...`；使用 `rg` 查找旧说法是否残留。 |
| 验收 | README 和 todo 对功能、限制、构建、测试的描述一致；没有把旧配置字段、远程移动、AI Agent、动态插件 / 不受限插件列为短期已承诺能力。 |
| 不做 | 不改功能代码；不重排 README 的全部结构；不新增 docs 目录。 |

## 6. P1 - 差异化能力

### P1-1 路径文本复制

| 字段 | 内容 |
|---|---|
| ID | `P1-1` |
| 目标 | 支持把当前文件路径、选中路径列表、当前目录作为纯文本复制给外部编辑器或 shell。 |
| 范围 | 新增纯文本剪贴板能力，不改变文件对象复制、剪切、粘贴语义。 |
| 关键改动 | 在平台层新增 `WriteClipboardText`；增加动作：复制当前文件路径、复制选中路径列表、复制当前目录；在菜单或快捷键中暴露；多路径使用换行分隔；远程路径使用 `xdssh://profile/path` 或当前 NetBox 约定的稳定文本格式。 |
| 测试 | 平台层文本写入函数用 fake command 或可替换函数测试；动作层覆盖当前项、无选择、多选、远程路径；运行 `go test ./...`。 |
| 验收 | 外部文本框可粘贴纯路径；文件剪贴板仍可用于系统文件粘贴；纯文本路径不会被误识别成内部文件剪贴板 payload。 |
| 不做 | 不实现路径格式模板；不做 URL/JSON/CSV 多格式复制。 |

### P1-2 Ctrl+R 历史反查

| 字段 | 内容 |
|---|---|
| ID | `P1-2` |
| 目标 | 让底部终端可以按子串快速找回历史命令。 |
| 范围 | 只做托管终端输入框的历史搜索 UI；PTY 独占程序内按键不接管。 |
| 关键改动 | `Ctrl+R` 进入 history search 状态；输入子串过滤历史；上下键或再次 `Ctrl+R` 循环候选；`Enter` 回填但不执行；`Esc` 退出并恢复原输入。 |
| 测试 | 历史排序、过滤、删除历史项后不可见、回填不执行、Esc 恢复 draft；运行 `go test ./...`。 |
| 验收 | 用户可增量搜索历史并回填；现有 suggestion popup 行为不回归；历史持久化格式不变。 |
| 不做 | 不做 fuzzy ranking；不改历史文件 schema；不做 shell 级 reverse-i-search 兼容。 |

### P1-3 独占 TUI 规则化

| 字段 | 内容 |
|---|---|
| ID | `P1-3` |
| 目标 | 减少全屏 TUI 自动识别误判，并让常见工具扩展更容易。 |
| 范围 | 只改独占 TUI 识别规则和测试；不改 PTY 渲染。 |
| 关键改动 | 从命令名集合升级为 command spec + predicate 规则；为 `vim/nvim/less/fzf/lazygit/yazi` 保留现有行为；让 `vim --version`、`nvim --headless`、`less --version` 等非交互参数不进独占；新增常见工具时必须配套规则测试。 |
| 测试 | 覆盖直接命令、带参数、shell wrapper、非交互参数、未知命令；运行 `go test ./...`。 |
| 验收 | 常见 TUI 仍可进入独占；非交互命令不占屏；新增规则不需要改散落条件判断。 |
| 不做 | 不做运行时用户自定义规则；不检测子进程最终 cwd。 |

### P1-4 跨本地/远程 F5 复制

| 字段 | 内容 |
|---|---|
| ID | `P1-4` |
| 目标 | 让用户能用 `F5` 在本地面板和远程 NetBox 面板之间复制文件。 |
| 范围 | 只实现复制；`F6` 移动继续明确不可用。 |
| 关键改动 | 复用现有远程下载和上传 tar 流；本地到远程走 upload；远程到本地走 download 后复制到目标；保留冲突策略；状态栏显示跨端复制进度；README 限制改为 `F5` 支持、`F6` 不支持。 |
| 测试 | fake NetBox download/upload；覆盖本地到远程、远程到本地、多选、冲突、失败清理、远端缺 tar 错误；运行 `go test ./...`。 |
| 验收 | `F5` 在一端本地一端远程时可复制；失败不会留下未清理临时目录；`F6` 给出明确提示且不执行远程删除。 |
| 不做 | 不实现跨端移动；不实现远程删除撤销；不支持 remote-to-remote 面板复制。 |

### P1-5 TUI 视觉与文案基线

| 字段 | 内容 |
|---|---|
| ID | `P1-5` |
| 目标 | 让高频 TUI 界面在小窗口和常见窗口尺寸下更稳、更清楚。 |
| 范围 | 状态栏、菜单文案、布局截断、主题 token 基线；不重写整体 UI 架构。 |
| 关键改动 | 统一状态栏短文案；修正菜单命名和快捷键标注；减少重复/超长提示；建立颜色 token 对照表；检查 80x24、120x32、宽屏下 header、panel、terminal、footer 不重叠。 |
| 测试 | 增加纯函数布局/截断测试；手动或脚本化运行三种窗口尺寸截图检查；运行 `go test ./...`。 |
| 验收 | 关键按钮和状态文本不互相覆盖；错误、成功、运行中状态文案一致；主题切换后可读性不下降。 |
| 不做 | 不接通 TOML 主题资产；不新增侧边栏；不做动画或 GUI 风格装饰。 |

## 7. P2 - 能力补齐

### P2-1 TOML 主题接通

| 字段 | 内容 |
|---|---|
| ID | `P2-1` |
| 目标 | 复用已有 TOML 主题资产，让 Theme 菜单可切换更多主题。 |
| 范围 | 在 P0-3 配置边界稳定后，将旧主题字段映射到新 UI token。 |
| 关键改动 | 定义 TOML theme 到 `xdfileTheme` 的最小映射；低对比度主题给出 fallback；Persona 主题继续保留；Theme 菜单合并 Persona 和 TOML 主题；保存后重启恢复。 |
| 测试 | 主题解析、缺字段 fallback、非法颜色 fallback、菜单 label、保存后重启恢复；运行 `go test ./...`。 |
| 验收 | TOML 主题可切换、可保存、可重启恢复；无不可读前景/背景组合。 |
| 不做 | 不改 TOML 文件格式；不实现在线主题下载；不把旧主题字段全量暴露为 API。 |

### P2-2A 压缩打包

| 字段 | 内容 |
|---|---|
| ID | `P2-2A` |
| 目标 | 支持把当前选择打包为 zip 或 tar.gz，补齐归档创建能力。 |
| 范围 | 只做打包 PR；解压单独排到 `P2-2B`。 |
| 关键改动 | 支持选中文件/目录生成 zip/tar.gz；输出文件名可确认；目标冲突复用现有冲突策略；支持取消和失败清理。 |
| 测试 | 打包内容校验、空选择、同名冲突、取消清理、大目录基础覆盖；运行 `go test ./...`。 |
| 验收 | 生成归档可被标准工具解开；取消或失败不留下半成品；现有预览不回归。 |
| 不做 | 不做解压；不支持 rar/7z 写入；不支持加密归档或高级压缩率选项。 |

### P2-2B 安全解压

| 字段 | 内容 |
|---|---|
| ID | `P2-2B` |
| 目标 | 安全解压 zip/tar.gz，阻止归档写出目标目录。 |
| 范围 | 只做解压 PR；依赖现有冲突策略和文件写入护栏。 |
| 关键改动 | 阻止路径穿越、绝对路径、危险 symlink/hardlink/special entry；解压前预检目标；复用 `Replace`、`Skip`、`Keep both`；使用 staging 目录提交结果；支持取消和失败清理。 |
| 测试 | 恶意归档路径穿越、绝对路径、symlink、hardlink、冲突策略、取消清理；运行 `go test ./...`。 |
| 验收 | 恶意归档不能写出目标目录；冲突行为可预测；失败不破坏已有文件。 |
| 不做 | 不支持 rar/7z 解压；不做密码归档；不实现远程直接解压。 |

### P2-3 侧边栏 / Pin

| 字段 | 内容 |
|---|---|
| ID | `P2-3` |
| 目标 | 提供可持久化的 Pin/Home 快速跳转，不破坏双面板主工作流。 |
| 范围 | 新增可隐藏侧边栏或弹出式 Pin 列表；优先 Pin/Home，磁盘导航后置。 |
| 关键改动 | 复用或收敛现有 pinned 数据结构；支持添加、删除、重命名 Pin；重启恢复；侧边栏隐藏时双面板布局保持原样。 |
| 测试 | Pin 文件读写、重复路径、删除不存在路径、布局宽度边界、重启恢复；运行 `go test ./...`。 |
| 验收 | Pin 后可跳转且持久化；隐藏侧边栏不影响现有双栏/终端布局。 |
| 不做 | 不做完整磁盘管理器；不接通旧 sidebar 全字段。 |

### P2-4 vim keymap

| 字段 | 内容 |
|---|---|
| ID | `P2-4` |
| 目标 | 支持用户切换到 vim 风格基础键位。 |
| 范围 | 先抽象动作到 keymap，再接 `vimHotkeys.toml` preset。 |
| 关键改动 | 建立 action 到 key bindings 映射；保留当前默认键位；vim preset 覆盖基础导航、打开、返回、选择、复制、粘贴、删除；冲突键必须有测试。 |
| 测试 | 默认 keymap 不变；vim preset 下基础操作可用；冲突检测；运行 `go test ./...`。 |
| 验收 | 切换 preset 后基础文件面板操作行为一致；README 只列出已接通键位。 |
| 不做 | 不承诺 `gg/G` 等未定义动作；不实现完整 Vim 模态编辑器。 |

### P2-5A 文件过滤

| 字段 | 内容 |
|---|---|
| ID | `P2-5A` |
| 目标 | 让用户能在当前目录内按文本过滤可见文件。 |
| 范围 | 只做当前面板过滤，不改目录扫描和排序核心语义。 |
| 关键改动 | 增加过滤输入状态；按文件名子串过滤当前面板；显示过滤状态和匹配数量；退出过滤后恢复原列表、光标和选择。 |
| 测试 | 空过滤、大小写策略、无匹配、多选状态、目录刷新后过滤状态；运行 `go test ./...`。 |
| 验收 | 过滤不会改变真实文件选择/排序语义；大目录下输入不卡顿到不可用。 |
| 不做 | 不做 fuzzy ranking；不做跨目录全文搜索；不接 zoxide。 |

### P2-5B 模糊搜索

| 字段 | 内容 |
|---|---|
| ID | `P2-5B` |
| 目标 | 在当前目录或近期路径中快速定位文件。 |
| 范围 | 独立于 `P2-5A` 的过滤 PR；只做可回退的搜索 UI。 |
| 关键改动 | 引入轻量 fuzzy matcher 或自实现可测试算法；候选列表支持键盘选择；`Enter` 跳转到匹配项；`Esc` 恢复。 |
| 测试 | ranking、空查询、重复文件名、目录刷新、Esc 恢复；运行 `go test ./...`。 |
| 验收 | 搜索结果稳定可解释；不会破坏默认导航性能。 |
| 不做 | 不做全盘索引；不做后台文件监听；不混入批量重命名。 |

### P2-5C 批量重命名

| 字段 | 内容 |
|---|---|
| ID | `P2-5C` |
| 目标 | 支持对选中文件批量重命名，并在执行前看见 diff 预览。 |
| 范围 | 只做本地文件批量重命名；远程批量重命名后置。 |
| 关键改动 | 生成 old/new 预览表；执行前校验冲突、非法文件名、空目标、大小写冲突；支持取消；失败时尽量保持已知状态并给出明确错误。 |
| 测试 | 冲突、非法文件名、预览、取消、部分失败、大小写敏感/不敏感文件系统差异；运行 `go test ./...`。 |
| 验收 | 没有预览确认就不会写盘；冲突和非法输入会被阻止；取消不改文件。 |
| 不做 | 不做远程批量重命名；不做正则 DSL 大而全；不混入过滤/搜索。 |

### P2-5D Zoxide 跳转

| 字段 | 内容 |
|---|---|
| ID | `P2-5D` |
| 目标 | 评估并接通 zoxide 目录跳转，提高常用路径导航效率。 |
| 范围 | 只做可选集成；系统未安装 zoxide 时必须静默降级。 |
| 关键改动 | 检测 `zoxide` 可执行文件；通过子进程查询候选目录；选择后切换当前面板目录；配置字段只暴露已实现开关。 |
| 测试 | fake zoxide 输出、未安装降级、路径不存在、选择跳转、错误提示；运行 `go test ./...`。 |
| 验收 | 未安装 zoxide 不报错；安装后可跳转；失败不会改变当前目录。 |
| 不做 | 不内置 zoxide；不修改用户 shell 配置；不放进 P0。 |

### P2-5E MD5 / 元数据展示

| 字段 | 内容 |
|---|---|
| ID | `P2-5E` |
| 目标 | 为选中文件提供可控的校验和或元数据查看能力。 |
| 范围 | 只做按需计算，不在默认列表渲染中同步计算。 |
| 关键改动 | 增加按需 MD5 计算动作；大文件显示进度或可取消；结果可复制；错误明确显示。 |
| 测试 | 小文件、空文件、大文件取消、权限错误、复制结果；运行 `go test ./...`。 |
| 验收 | 默认浏览性能不下降；用户主动触发后能得到可复制结果。 |
| 不做 | 不默认扫描目录 MD5；不做多算法矩阵；不放进 P0。 |

## 8. 研究项路线

研究项不直接升级为短期功能承诺。默认按“贴合 TUI-first 文件管理器核心价值、风险可控、能独立验收”排序：

1. 独占模式退出后同步 cwd
2. AI 助手 v1
3. 插件系统
4. 多标签 / 分屏

总体策略：每个方向先做 ADR 或实验 PR，再按 gate 决定是否进入功能 PR。

执行规则：

- 每个方向先做 ADR 或实验 PR，再按 gate 决定是否进入功能 PR。
- ADR PR 不改运行行为，不把功能写成已实现。
- 功能 PR 必须新增状态机或安全边界测试。
- AI 默认关闭；插件只加载 `enabled: true` 的外部命令插件。

### R1 独占模式退出后同步 cwd

#### R1-1 ADR: cwd 同步机制决策

| 字段 | 内容 |
|---|---|
| ID | `R1-1` |
| 目标 | 决定独占 TUI 退出后 cwd 同步的可行机制和安全边界。 |
| 范围 | 只做 ADR 文档；对比现有 prompt 解析、OSC 7、shell hook、临时文件同步。 |
| 关键改动 | 结论默认选 OSC 7 监听优先，shell hook 作为 opt-in fallback；明确只支持本地 PTY shell；记录普通子进程最终 cwd 不可通用获取。 |
| 测试 | `go test ./...`；`go vet ./...`；使用 `rg` 确认 README 或 todo 没把 cwd 同步写成已实现功能。 |
| 验收 | ADR 能回答为什么选 OSC 7、何时用 hook、哪些 shell 或场景不覆盖；后续功能 PR 不需要重新做机制选择。 |
| 不做 | 不改 PTY 输出解析；不自动修改用户 shell 配置；不支持远程 NetBox 命令 cwd 同步。 |

#### R1-2 PTY OSC 7 cwd 捕获

| 字段 | 内容 |
|---|---|
| ID | `R1-2` |
| 目标 | 从本地 PTY 输出流捕获 OSC 7 cwd 事件，并同步当前活动面板。 |
| 范围 | 只处理本地 PTY shell 输出；仅当路径有效且为本地目录时更新。 |
| 关键改动 | 在 PTY 输出处理链路解析 OSC 7 file URL；解析成功后更新 `terminal.Cwd` 和当前 active panel cwd；非法 URL、远程路径、不可访问目录静默忽略。 |
| 测试 | OSC 7 URL 解析测试；非法 URL、非 file scheme、远程 host、不可访问目录测试；当前面板 reload 状态机测试；运行 `go test ./...`。 |
| 验收 | 支持 OSC 7 的 shell 改变 cwd 后面板可跟随；无效事件不改变当前目录；不影响现有 prompt 解析。 |
| 不做 | 不解析任意 escape 序列；不支持远程 cwd；不保证每个 TUI 程序退出都发 OSC 7。 |

#### R1-3 opt-in shell hook

| 字段 | 内容 |
|---|---|
| ID | `R1-3` |
| 目标 | 为不发 OSC 7 的 shell 提供用户可手动安装的 cwd 同步 hook。 |
| 范围 | 只提供文档和可复制 hook 片段；运行时只消费 hook 输出或同步文件。 |
| 关键改动 | README 增加 bash、zsh、PowerShell 示例；hook 输出 OSC 7 或写入 Xdfile 指定同步文件；明确卸载方式和限制。 |
| 测试 | hook 生成函数测试或文档 snippet smoke test；同步文件路径解析测试；运行 `go test ./...` 和 `go vet ./...`。 |
| 验收 | 用户可复制 hook 后让普通 shell cwd 同步；未安装 hook 时行为不变；文档不承诺自动修改 shell 配置。 |
| 不做 | 不自动写 `.bashrc`、`.zshrc`、PowerShell profile；不提供 shell 插件管理器。 |

### R2 AI 助手 v1

#### R2-1 ADR: AI 助手边界与安全模型

| 字段 | 内容 |
|---|---|
| ID | `R2-1` |
| 目标 | 明确 AI 助手 v1 只做 NL2Shell 草稿生成，不做自动 agent。 |
| 范围 | 只做 ADR；定义输入、输出、安全策略和禁用默认。 |
| 关键改动 | 规定生成结果只填入底部终端输入框；不自动执行；不读取密钥文件；不上传文件内容；危险命令需要确认文案。 |
| 测试 | `go test ./...`；`go vet ./...`；`rg` 确认 README 只声明 NL2Shell 草稿边界，不写成自主 AI Agent。 |
| 验收 | ADR 列出危险命令分类：删除、覆盖、权限变更、网络上传、包管理、远程执行；后续 PR 有清晰安全边界。 |
| 不做 | 不接 OpenAI 或其他 provider；不实现聊天面板；不做自主文件操作。 |

#### R2-2 AI 配置与 provider 抽象

| 字段 | 内容 |
|---|---|
| ID | `R2-2` |
| 目标 | 为 AI 助手建立默认关闭的最小配置和可替换 provider 接口。 |
| 范围 | 只接入配置、接口和 fake provider；不做真实网络 provider。 |
| 关键改动 | 新增白名单配置 `ai_enabled`、`ai_provider`、`ai_model`、`ai_api_key_env`；API key 只从环境变量读取；provider 输入只含用户请求、cwd、选中路径摘要。 |
| 测试 | 配置默认关闭、环境变量缺失、fake provider 调用参数、不会传文件内容；运行 `go test ./...`。 |
| 验收 | 未配置时菜单或入口提示未启用；provider 可被测试替换；旧配置缺字段不影响启动。 |
| 不做 | 不保存 API key；不读取文件正文；不发真实 API 请求。 |

#### R2-3 NL2Shell 输入框集成

| 字段 | 内容 |
|---|---|
| ID | `R2-3` |
| 目标 | 让用户输入自然语言并把生成命令回填到底部终端输入框。 |
| 范围 | 新增 AI 输入 modal、菜单入口和回填流程；只在 `ai_enabled` 为 true 时可用。 |
| 关键改动 | 新增 `Terminal -> AI command` 或等价入口；调用 provider 后显示命令预览；确认后写入终端输入框但不执行。 |
| 测试 | 成功生成、取消、provider error、未启用降级、回填不执行；运行 `go test ./...`。 |
| 验收 | 用户能从自然语言得到命令草稿；终端 busy 或独占 TUI 时不抢输入；Enter 不会被自动发送。 |
| 不做 | 不自动运行命令；不做多轮对话；不把 AI 接进文件操作菜单。 |

#### R2-4 脱敏与审计测试

| 字段 | 内容 |
|---|---|
| ID | `R2-4` |
| 目标 | 给 AI 输入输出路径加脱敏和危险命令审计护栏。 |
| 范围 | 只做本地规则、测试和确认文案；不做云端安全服务。 |
| 关键改动 | 脱敏环境变量名、token-like 字符串、用户主目录片段；危险命令预览前标记并要求确认；审计只保存本地状态或不保存。 |
| 测试 | token 脱敏、用户名路径脱敏、危险命令分类、误判保守策略、确认后仍只回填；运行 `go test ./...`。 |
| 验收 | 明显敏感值不会进入 provider 输入；危险命令不会静默进入输入框；普通命令不被过度阻断。 |
| 不做 | 不做安全沙箱；不做命令执行拦截器；不保证识别所有危险 shell 语法。 |

### R3 插件系统

#### R3-1 ADR: 插件边界

| 字段 | 内容 |
|---|---|
| ID | `R3-1` |
| 目标 | 决定插件系统 v1 的隔离方式和能力边界。 |
| 范围 | 只做 ADR；不实现插件加载。 |
| 关键改动 | 默认选外部命令插件，通过 JSON stdin/stdout 通信；不使用 Go dynamic plugin；插件 v1 只读上下文并返回菜单项或终端命令草稿。 |
| 测试 | `go test ./...`；`go vet ./...`；README 或 todo 只声明外部命令插件 v1 边界，不写成动态 Go 插件或不受限插件。 |
| 验收 | ADR 明确插件能力、禁止能力、超时策略和错误处理；后续实现无需重新选择协议。 |
| 不做 | 不做二进制 ABI 插件；不允许插件直接改文件；不开放内部 Go API。 |

#### R3-2 插件 manifest 与发现

| 字段 | 内容 |
|---|---|
| ID | `R3-2` |
| 目标 | 定义插件 manifest 并发现显式启用的外部命令插件。 |
| 范围 | 只做 manifest 解析、目录扫描和启用过滤；不执行插件动作。 |
| 关键改动 | 定义 `xdfile-plugin.json` 字段：名称、版本、命令、能力、超时；插件目录为 `xdfile-data/plugins/`；未知插件默认禁用。 |
| 测试 | 有效 manifest、缺字段、非法 timeout、未启用插件、命令不存在；运行 `go test ./...`。 |
| 验收 | 只有显式启用且 manifest 有效的插件进入运行时列表；错误插件不影响主程序启动。 |
| 不做 | 不下载插件；不自动启用插件；不执行插件命令。 |

#### R3-3 插件菜单接入

| 字段 | 内容 |
|---|---|
| ID | `R3-3` |
| 目标 | 将启用插件以受限动作形式接入 TUI 菜单。 |
| 范围 | 在 F2 User Menu 或 `Panels -> Plugins` 暴露插件动作；只允许安全输出。 |
| 关键改动 | 插件可返回显示文本、填入终端命令草稿、复制文本三类动作；所有动作需用户确认；错误显示在 modal 或状态栏。 |
| 测试 | 插件返回文本、命令草稿、复制文本、非法动作、用户取消；运行 `go test ./...`。 |
| 验收 | 插件能扩展菜单但不能直接删除、移动、上传或改配置；主 TUI 不因插件失败崩溃。 |
| 不做 | 不开放任意文件写入；不允许后台常驻插件；不做插件 UI 自绘。 |

#### R3-4 插件隔离与超时测试

| 字段 | 内容 |
|---|---|
| ID | `R3-4` |
| 目标 | 确保插件进程错误、超时和无效输出都被隔离。 |
| 范围 | 只补执行隔离、timeout 和错误处理测试。 |
| 关键改动 | 子进程超时强制结束；无效 JSON、退出码非零、插件不存在都转为用户可读错误；限制输出大小。 |
| 测试 | 超时进程、无效 JSON、大输出、非零退出、命令不存在；运行 `go test ./...` 和 `go vet ./...`。 |
| 验收 | 插件异常不会阻塞 TUI；错误可见且不污染文件剪贴板或面板状态。 |
| 不做 | 不实现 OS 级 sandbox；不做权限弹窗系统。 |

### R4 多标签 / 分屏

#### R4-1 ADR: Tabs vs Split 决策

| 字段 | 内容 |
|---|---|
| ID | `R4-1` |
| 目标 | 决定多工作区形态，避免直接进入高复杂度分屏。 |
| 范围 | 只做 ADR；对比 tabs、任意 split、现有双栏扩展三种方案。 |
| 关键改动 | 默认选择多 workspace tab；每个 tab 包含双栏面板、终端 cwd、过滤和搜索状态；配置、主题、NetBox、Pin、命令菜单全局共享。 |
| 测试 | `go test ./...`；`go vet ./...`；确认 README 不声明多标签已实现。 |
| 验收 | ADR 明确为什么不做任意嵌套分屏；后续状态模型可按 workspace 推进。 |
| 不做 | 不实现 UI；不做持久化；不做任意布局树。 |

#### R4-2 Tab 状态模型

| 字段 | 内容 |
|---|---|
| ID | `R4-2` |
| 目标 | 把当前单工作区状态抽象为可复制的 `xdfileWorkspace`。 |
| 范围 | 只做运行时状态模型；暂不持久化 tabs。 |
| 关键改动 | 顶层 model 持有 workspace 列表和 active workspace index；迁移面板、终端 cwd、过滤、搜索、quick view 等 per-workspace 状态；全局状态保持在 model。 |
| 测试 | 新建 workspace 默认值、切换 workspace 保留各自 cwd 和选择、全局主题与 Pin 不重复；运行 `go test ./...`。 |
| 验收 | 单 tab 行为与现有行为一致；多 tab 状态互不串扰；旧 layout 能按单 workspace 启动。 |
| 不做 | 不画 tab header；不保存 tab 列表；不支持分屏。 |

#### R4-3 Tab UI 与快捷键

| 字段 | 内容 |
|---|---|
| ID | `R4-3` |
| 目标 | 在 TUI 中提供新建、关闭和切换 workspace tab 的基础交互。 |
| 范围 | 顶部 header、菜单动作、快捷键和状态提示；不改变双栏主布局。 |
| 关键改动 | Header 显示 tab 标题和序号；新增 new、close、next、previous tab 动作；关闭最后一个 tab 时重置为空 tab 而不退出程序。 |
| 测试 | 新建、关闭、切换、关闭最后 tab、80x24 和 120x32 header 截断；运行 `go test ./...`。 |
| 验收 | 用户可在多个工作区间切换；现有 F2、NetBox、终端快捷键不回归；小窗口不重叠。 |
| 不做 | 不做拖拽排序；不做任意分屏；不持久化 tab。 |

#### R4-4 Tab 持久化可选 PR

| 字段 | 内容 |
|---|---|
| ID | `R4-4` |
| 目标 | 在 tab 状态稳定后保存和恢复 workspace cwd 列表。 |
| 范围 | 只保存最小 tab cwd 和 active index；兼容旧 layout。 |
| 关键改动 | layout 新增可选 tabs 字段；缺字段按单 tab 启动；无效路径回退到默认目录；保存 setup 时写入。 |
| 测试 | 旧 layout 兼容、新 layout round-trip、无效路径回退、active index 越界；运行 `go test ./...`。 |
| 验收 | 重启后可恢复 tab 列表；旧用户配置不需要迁移即可启动。 |
| 不做 | 不保存完整滚动、搜索、modal 状态；不同步远程连接会话。 |

## 9. 文档改动验证

文档改动后运行：

```bash
go test ./...
go vet ./...
bash scripts/build.sh
rg -n "当前 \\*_test.go|为 0|Panel-to-panel SSH|LoadConfigFile|go vet|xdfile-manager" todo.md README.md src .github scripts
rg -n '^### R|^#### R|^\| ID \| `R' todo.md
```

验收标准：

- `todo.md` 中每个 P0/P1/P2 条目都能作为单独 PR 或明确拆分后的单独 PR 执行。
- `todo.md` 中每个 R 研究项都拆成 ADR 或功能 PR，并包含目标、范围、关键改动、测试、验收、不做。
- 没有旧测试数量等过期事实。
- 没有把旧配置字段、跨端 `F6`、远程删除撤销、AI Agent、动态插件 / 不受限插件列为短期已实现或短期承诺。
- 文档能回答：为什么做、改哪里、怎么测、什么算完成、什么明确不做。

*最后更新：2026-06-08*
